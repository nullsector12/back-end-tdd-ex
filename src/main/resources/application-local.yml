server:
  port: 20000

spring:
  datasource:
    hikari:
      jdbc-url: jdbc:mysql://localhost:23306/piece_sale?useSSL=false&useUnicode=yes&characterEncoding=UTF-8&allowMultiQueries=true&serverTimezone=Asia/Seoul&zeroDateTimeBehavior=convertToNull&allowPublicKeyRetrieval=TRUE
      username: root
      password: pw_mysql

      pool-name: piece-sale-command-local
      minimum-idle: 10 # performance first
      maximum-pool-size: 10 # 10 default